# claude-skills

ชุด Claude Code skill สำหรับดูแล **คลังความรู้ส่วนตัว (second brain)** — สกัดเฉพาะ *กระบวนการ* ของงานที่ทำซ้ำ ๆ
ออกมาให้ใช้ข้ามโปรเจกต์ได้ โดยไม่ผูกกับโครงสร้างโฟลเดอร์ของคลังใดคลังหนึ่ง

## ที่มา

สกัดครั้งแรกจากคลัง `second-brain` ส่วนตัว (ระบบ 3-tree ที่ควบคุมด้วย `CLAUDE.md` ต่อ tree) เมื่อ 2026-08-12
ตอนนั้นยังเป็น "ตัวอย่าง pattern ที่ต้องแก้ path เองก่อนใช้" ต่อมาเขียนใหม่ทั้งหมดให้พกพาได้จริง —
เหตุผลและทางเลือกที่พิจารณาแล้วทิ้ง อยู่ใน [`docs/adr/0001-generalize-skills-for-portability.md`](docs/adr/0001-generalize-skills-for-portability.md)

## รายการ skill

| skill | ทำอะไร |
|---|---|
| [`init-brain/`](init-brain/) | สร้างคลังใหม่ (สัมภาษณ์ → scaffold) หรือตรวจสุขภาพคลังเดิมด้วยเกณฑ์ 11 องค์ประกอบ → รายงาน → แก้เฉพาะที่อนุมัติ |
| [`digest/`](digest/) | นำเข้าเอกสารดิบจากโฟลเดอร์พักเข้าคลัง — อ่านเนื้อหาจริง, ผ่านด่านความเป็นส่วนตัว/ลิขสิทธิ์, เขียน digest, ลงทะเบียน |
| [`new-subject/`](new-subject/) | เปิดหัวข้อใหม่ในคลัง — skeleton, ย่อยเอกสารตั้งต้น, เขียนไฟล์ภาพรวม, ลงทะเบียนใน index |
| [`make-exam/`](make-exam/) | ออกข้อสอบตามลำดับที่ห้ามสลับ — blueprint → items → answer key → rubric |
| [`update-portfolio/`](update-portfolio/) | ซิงก์เอกสารสรุปที่คร่อมหลายแหล่ง — เทียบทีละหัวข้อกับต้นทาง + บันทึกประวัติการตัดสินใจ |
| [`ship/`](ship/) | ปิดงานเป็น commit ที่สะอาด — จัดกลุ่มตามหัวเรื่อง, สแกน diff หาข้อมูลส่วนบุคคล, เขียน message ผ่านไฟล์ |

เริ่มจากศูนย์ให้ใช้ `init-brain` ก่อน — มันสร้าง `CLAUDE.md`, index, templates และสคริปต์ตรวจ
ซึ่งเป็น "สิ่งที่ skill ตัวอื่นไปอ่านตอนรัน"

## แพทเทิร์นที่ใช้ (สำคัญกว่าตัวไฟล์เอง)

ทุก skill ในนี้ตามแพทเทิร์นเดียวกัน:

1. **Runtime discovery แทน hard-code** — ขั้นแรกของทุก skill คืออ่าน `CLAUDE.md` และโครงสร้างจริง
   ของโปรเจกต์ปลายทางก่อนตัดสินใจอะไรทั้งสิ้น **กติกาของโปรเจกต์ override ค่า default ของ skill เสมอ**
   คลุมเครือเมื่อไหร่ให้ถามผู้ใช้ ไม่ใช่เดา — จึงย้ายไปวางที่ไหนก็ใช้ได้โดยไม่ต้องแก้ไฟล์
2. **skill = checklist เรียงลำดับ ไม่ใช่ที่เก็บกติกา** — และเป็น checklist เดี่ยว ไม่เรียก skill อื่นมาต่อกัน
3. **จบด้วยขั้นตรวจแบบมีเงื่อนไข** — โปรเจกต์มีสคริปต์ตรวจก็รัน ไม่มีก็ตรวจตาม checklist ที่ skill ระบุไว้
   ไม่ปล่อยให้ "รู้สึกว่าเสร็จ" เป็นเกณฑ์จบงาน
4. **ภาษาอังกฤษเป็น default** ในเนื้อหา skill ส่วน trigger ภาษาไทยเก็บไว้ใน `description`
   เพื่อให้ Claude เรียก skill ถูกตัวเมื่อผู้ใช้พิมพ์ไทย
5. **Progressive disclosure** — ความรู้อ้างอิงที่ไม่ใช่ทุกครั้งต้องใช้ แยกเป็นไฟล์ข้างเคียงแล้วชี้จาก SKILL.md
   (`init-brain/BRAIN-ANATOMY.md`, `make-exam/ASSESSMENT-REFERENCE.md`) — ตัว checklist หลักจะได้ไม่บวม

## วิธีใช้

คัดลอกโฟลเดอร์ skill ที่ต้องการไปไว้ที่ `<โปรเจกต์>/.claude/skills/<ชื่อ>/` แล้วใช้ได้เลย ไม่ต้องแก้ path

ถ้าโปรเจกต์ปลายทางยังไม่มี `CLAUDE.md` หรือโครงสร้างคลัง skill จะถามหาข้อมูลที่จำเป็นระหว่างทาง —
ถ้าไม่อยากตอบซ้ำทุกครั้ง ให้รัน `/init-brain` สักครั้งเพื่อวางกติกาไว้ให้ตัวอื่นอ่าน
