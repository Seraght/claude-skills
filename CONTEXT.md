# claude-skills

ไฟล์นี้เป็น **glossary** ของศัพท์ที่ทุก skill ใน `claude-skills` ใช้ร่วมกัน — ไม่ใช่ spec
และไม่ใช่ที่เก็บรายละเอียดการทำงาน (ดู [`README.md`](README.md) สำหรับแพทเทิร์น
และ [`docs/adr/`](docs/adr/) สำหรับเหตุผลของการตัดสินใจ)

> **คำว่า "คลัง" ในทุกเอกสารของ repo นี้ หมายถึง Vault เท่านั้น** — คือสิ่งที่ skill เข้าไปทำงานด้วย
> ตัว repo นี้เรียกด้วยชื่อจริงว่า `claude-skills` (หรือ "ปลั๊กอิน" เมื่อพูดถึงรูปแบบที่แจกจ่าย)
> ส่วนของหนึ่งชิ้นในนี้เรียกว่า "skill" เฉย ๆ ตามชื่อไฟล์ `SKILL.md`

## Language

### สิ่งที่ skill ทำงานด้วย

**Vault**:
คลังความรู้หนึ่งก้อนที่ skill เข้าไปทำงานด้วย — มีกติกาของตัวเองและมีขอบเขตชัดเจน
_Avoid_: repo, โปรเจกต์, second brain (คำหลังหมายถึงแนวคิด ไม่ใช่ก้อนไฟล์ก้อนหนึ่ง)

**Host project**:
โปรเจกต์ปลายทางที่ skill ถูกนำไปวาง — คำนี้ใช้เมื่อต้องการเน้นว่า "ไม่ใช่คลังที่ skill เกิดมา"
_Avoid_: target repo, โปรเจกต์ผู้ใช้

**Constitution**:
ไฟล์กติกาที่ agent ต้องอ่านก่อนลงมือ — เป็นแหล่งความจริงของ vault นั้น ชื่อมาตรฐานที่ `init-brain` สร้างคือ
`AGENTS.md` (ชื่อกลางที่ agent หลายค่ายอ่าน) โดยมี `CLAUDE.md` บรรทัดเดียววางข้างกันเพื่อชี้มาหา
ส่วน vault ที่มีอยู่ก่อนแล้วและใช้ `CLAUDE.md` อย่างเดียว ถือว่าครบแล้ว
_Avoid_: config, กติกาต้นทาง, rules file

**Staging**:
โฟลเดอร์พักของที่รับเข้ามาแล้วแต่ยังไม่ได้จัดเก็บ (ปกติ `temp/`) — แยก "รับเข้า" ออกจาก "ตัดสินใจว่าเก็บที่ไหน"
_Avoid_: inbox, temp folder, ที่พักไฟล์

**Digest**:
สรุปที่เขียนขึ้นจากเอกสารต้นฉบับ และเป็นสิ่งที่คลังเก็บไว้จริง — ต้นฉบับอาจถูกเก็บหรือไม่ก็ได้
_Avoid_: summary, ย่อความ, บทคัดย่อ

**Roll-up document**:
เอกสารเดี่ยวที่สรุปสถานะซึ่งอยู่กระจายในที่อื่น — เก่าทันทีที่ต้นทางเปลี่ยน คุณค่าของมันคือคนอ่านเชื่อได้โดยไม่ต้องเปิดต้นทาง
_Avoid_: dashboard, สรุปรวม, ไฟล์ภาพรวม

**Gap marker**:
สัญลักษณ์ใน index ที่แยก "มีแล้ว" ออกจาก "ตั้งใจจะมีแต่ยังไม่เขียน" — ทำให้คลังเป็นรายการงาน ไม่ใช่แค่ที่เก็บ
_Avoid_: TODO, สถานะ, placeholder

### วิธีที่ skill ถูกออกแบบ

**Runtime discovery**:
การที่ skill อ่าน constitution และโครงสร้างจริงของ host project ตอนรัน แทนที่จะมีโครงสร้างฝังอยู่ในตัว
_Avoid_: auto-detect, ตรวจโครงสร้าง

**Skill default**:
ค่าตั้งต้นที่ skill ใช้เมื่อ host project ไม่ได้กำหนดไว้ — **กติกาของ host project ชนะเสมอเมื่อมีทั้งคู่**
_Avoid_: fallback, ค่าเริ่มต้น

**Conditional verify**:
ขั้นตรวจปิดท้ายที่ปรับตามสิ่งที่ host project มี — มีสคริปต์ตรวจก็รัน ไม่มีก็ตรวจตาม checklist ที่ skill ระบุ
_Avoid_: validation step, ขั้นตรวจสอบ

**Gate**:
เงื่อนไขที่ต้องผ่านก่อนของจะเข้าคลังได้ และ **หยุดงานเมื่อไม่ผ่าน** (ความเป็นส่วนตัว, ลิขสิทธิ์)
_Avoid_: check, ด่านตรวจ, filter

**Greenfield / Audit**:
สองโหมดของ `init-brain` — greenfield คือสร้างคลังใหม่จากศูนย์, audit คือให้คะแนนคลังที่มีอยู่แล้วแล้วเสนอแก้
_Avoid_: init/check, สร้าง/ตรวจ

**Progressive disclosure**:
การย้ายความรู้อ้างอิงที่ไม่ใช่ทุกครั้งต้องใช้ ออกไปเป็นไฟล์ข้างเคียงแล้วชี้จาก `SKILL.md`
_Avoid_: แยกไฟล์, modularize

### กลุ่มของ skill

**งานกับคลังความรู้**:
กลุ่มของ skill ที่ผลลัพธ์เมื่อทำงานเสร็จคือ *เนื้อหาในคลัง* — โครงสร้าง, digest, index, roll-up
_Avoid_: กลุ่ม vault, งาน second brain

**งานสอนและวัดผล**:
กลุ่มของ skill ที่ผลลัพธ์เมื่อทำงานเสร็จคือ *เอกสารที่คนอื่นเอาไปเรียนหรือใช้ประเมิน* — ข้อสอบ, เฉลย, rubric
_Avoid_: กลุ่มการศึกษา, งานอาจารย์

**เกณฑ์การจัดกลุ่ม**:
การตัดสินว่า skill ตัวหนึ่งอยู่กลุ่มไหน โดยดูที่ *สิ่งที่มันเปลี่ยนแปลงเมื่อทำงานเสร็จ* ไม่ใช่หัวข้อของงาน
— ตัวเกณฑ์เต็มอยู่ใน [`README.md`](README.md) ไฟล์นี้เก็บเฉพาะชื่อกลุ่ม
_Avoid_: หมวดหมู่, taxonomy
